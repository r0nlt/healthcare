# add_subdirectory(validation) # Files were moved to verification directory
add_subdirectory(unit)
add_subdirectory(verification)
# add_subdirectory(integration) # Will be enabled when integration tests are implemented 