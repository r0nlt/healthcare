add_subdirectory(core)
add_subdirectory(testing)
# add_subdirectory(math) # Will be enabled when math tests are implemented
add_subdirectory(inference)
# add_subdirectory(inference) # Will be enabled when inference tests are implemented 